interface NetworkCardProps {
    userId: string;
    fullName: string;
    username: string;
    profilePhotoPath: string;
    connected: boolean;
    requested: boolean;
}

export default NetworkCardProps;